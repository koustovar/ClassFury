import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Light Mode)
  static const primary = Color(0xFF1E40AF); // Deep Blue 800
  static const primaryLight = Color(0xFF3B82F6); // Blue 500
  static const primaryDark = Color(0xFF1E3A8A); // Blue 900

  static const secondary = Color(0xFF7C3AED); // Violet 600
  static const secondaryLight = Color(0xFFA78BFA); // Violet 400

  static const accent = Color(0xFFF59E0B); // Amber 500

  // Neutral Colors (Light Mode)
  static const background = Color(0xFFF8FAFC); // Slate 50
  static const surface = Color(0xFFFFFFFF);
  static const card = Color(0xFFFFFFFF);

  // ── Dark Mode: Red × Orange × Black ──────────────────────────────────────
  static const backgroundDark = Color(0xFF000000); // Pure Black
  static const surfaceDark = Color(0xFF111111); // Very Dark Grey
  static const cardDark = Color(0xFF1A1A1A); // Dark Card

  static const primaryDarkTheme = Color(0xFFE11D48); // Bold Red   (rose-600)
  static const secondaryDarkTheme =
      Color(0xFFFB923C); // Bright Orange (orange-400)
  static const accentDarkTheme = Color(0xFFFF6B00); // Deep Orange accent

  // Dark Text
  static const textPrimaryDark = Color(0xFFFFFFFF); // Pure White
  static const textSecondaryDark = Color(0xFFA1A1AA); // Zinc 400
  static const textHintDark = Color(0xFF52525B); // Zinc 600

  // Dark UI elements
  static const dividerDark = Color(0xFF262626); // Neutral 800
  static const borderDark = Color(0xFF3F3F46); // Zinc 700
  // ─────────────────────────────────────────────────────────────────────────

  // Semantic Colors (shared)
  static const error = Color(0xFFDC2626); // Red 600
  static const success = Color(0xFF10B981); // Emerald 500
  static const warning = Color(0xFFF59E0B); // Amber 500
  static const info = Color(0xFF0EA5E9); // Sky 500

  // Feature Specific Colors
  static const teal = Color(0xFF0D9488);
  static const orange = Color(0xFFEA580C);
  static const purple = Color(0xFF7C3AED);
  static const green = Color(0xFF16A34A);
  static const red = Color(0xFFDC2626);
  static const blue = Color(0xFF2563EB);

  // Text Colors (Light Mode)
  static const textPrimary = Color(0xFF0F172A); // Slate 900
  static const textSecondary = Color(0xFF64748B); // Slate 500
  static const textPlaceholder = Color(0xFF94A3B8); // Slate 400

  // UI Elements (Light Mode)
  static const divider = Color(0xFFE2E8F0); // Slate 200
  static const border = Color(0xFFCBD5E1); // Slate 300

  // Common Colors
  static const white = Colors.white;
  static const grey = Colors.grey;
}
